{
	"name": "pi-extensions",
	"version": "0.1.0",
	"description": "ARCHIVED — Original umbrella repo for pi coding agent packages. Each package has been split into its own repository.",
	"private": true,
	"keywords": ["pi-package", "archived"]
}