{
	"name": "pi-memory",
	"version": "0.2.0",
	"description": "OpenViking-backed persistent memory for the pi coding agent: openviking_* tools plus per-turn memory recall injection. Port of vikingbot's OpenViking integration.",
	"type": "module",
	"keywords": ["pi-package"],
	"pi": {
		"extensions": ["./extensions"]
	},
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": "*",
		"typebox": "*"
	}
}
